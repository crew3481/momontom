const dDaySection = document.querySelector(".d-day");
const dDayForm = dDaySection.querySelector(".d-day__form");
const dDayInput = dDaySection.querySelector(".d-day__input");
const dDayDatetime = dDaySection.querySelector(".d-day__datetime");
const dDayName = dDaySection.querySelector(".d-day__name");
const dDay = dDaySection.querySelector(".d-day__day");

/*
    const dDaySubmit = () => {}

    1. 이 함수는 사용자가 d-day 전송 이벤트를 발생 시켰을때 반응하는 함수입니다.

    const calDay = () => {}

    1. 이 함수는 d-day를 계산하는 함수입니다.
    2. Keyword - 자바스크립트 Date
    3. Date를 이용해 날짜를 빼거나 더할줄 알아야 한다.
    4. 사용자로 부터 받아온 정보를 적절히 이용할 줄 알아야 한다.
    5. 몇일이 지났거나 몇일이 남았는지 계산해서 " 리턴 "합니다.

    const printDay = (day, name) => {}

    1. 이 함수는 계산된 d-day를 출력하는 함수입니다.
    2. 출력예시 - 이사 d-30 

    # 실행 순서

    1. dDaySubmit();
    2. calDay();
    3. printDay(day);

    예시 

    const dDaySubmit = (e) =>{
        // 기본 동작 실행 취소
        const dDay = calDay();
        printDay(dDay);
        // 입력받았던 폼 숨기기
    }
*/
